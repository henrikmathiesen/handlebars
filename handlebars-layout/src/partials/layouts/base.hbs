<!doctype html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="{{#if title}}{{title}}{{else}}Base Page Title{{/if}} - My Site">
    <title>
        {{#if title}} {{title}} {{else}} Base Page Title {{/if}}
    </title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/normalize/7.0.0/normalize.min.css"> 
    {{!-- block include with empty fallback --}}
    {{#> styles-block}}{{/styles-block}}
</head>

<body>
    <h1>{{#if title}} {{title}} {{else}} Base Page Title {{/if}}</h1>
    {{!-- normal block includes must exists before compilation --}}
    {{> hero-block}}

    <footer>
        {{!-- Block include with fallback. The 'includes/footer' partial is the default content, but can be overridden. --}}
        {{#> footer-block}} {{> includes/footer }} {{/footer-block}}
    </footer>
    
</body>

</html>
